# Production Environment Variables
# Copy this file to .env.production and fill in the values

# CRITICAL: Change this to a strong random secret (at least 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=

# Your production domain (change if using custom domain)
CORS_ORIGIN=https://ec2-18-218-0-115.us-east-2.compute.amazonaws.com

# Admin user email for initial setup
ADMIN_EMAIL=admin@tienda.com

# Email for Let's Encrypt SSL certificates (used in Nginx Proxy Manager)
SSL_EMAIL=your-email@example.com
